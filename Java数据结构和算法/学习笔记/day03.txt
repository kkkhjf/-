1、递归
	自己调用自己，每次调用的时候传入不同的变量；
	递归的规则
		当程序执行到一个方法时，就会开辟一个独立的空间；
		方法的局部变量是独立的，不会互相影响；
		如果方法中使用的是引用类型变量，就会共享该引用类型的数据；
		递归必须向退出递归的条件逼近，否则就是无限递归；
		当一个方法执行完毕，或者遇到return就会返回，谁调用就将结果返回给谁；
	注：递归的每一步就是要把所有的可能性都跑一遍；
		递归尽量从最后一步开始考虑；
		在求f(n, other variables)的时候，你就默认f(n -1, other variables)已经被求出来了——至于怎么求的，这个是计算机通过回溯求出来的;
		递归的关键
			递归的结束条件（不写会死循环，TLE）；	
			递归最后一层和其他有关系的层的关系怎样用非递归函数来表达；
2、排序算法
	排序分类
		内部排序
			将所有数据都加载到内部存储器中进行排序；
		外部排序
			数据量过大，需要借助排布存储器进行排序；
	冒泡排序
		每轮排序时将最大的值依次移动到最右端；
	选择排序
		每轮排序中，找到最小的一个值，将它与左边的数进行交换；
	插入排序
		把n个待排序的元素堪称一个有序表和一个无序表，排序时每次从无序表中去一个元素把它按照顺序插入到有序表中；
	希尔排序
		
	快速排序
	
	归并排序
		