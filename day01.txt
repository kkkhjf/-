1、Java关键字
    Java关键字中包含所有Java指定的描述词语。
2、标识符
    数字字母下划线；
    不能用数字开头；
    不能使用Java关键字，但是可以包含Java关键字。
3、Java命名规范
    包名全部小写；
    类名、接口名用大驼峰写法；
    变量名、方法名用小驼峰写法；
    常量全部用大写。
4、Java编译过程
    .Java文件在编译后产生.class文件，即字节码文件，通过对字节码文件的读取实现Java代码。
